node_modules/

# executor workspaces and transcripts are machine-local; records and diffs are committed
artifacts/**/workspace/
artifacts/**/transcript.md

# repo-shaped runs snapshot the whole scaffold into output/; the result.yaml is the
# record, not hundreds of unchanged boilerplate files
artifacts/**/output/

# workspace seeds are regenerated, not versioned (task notes record how)
templates/

*.tsbuildinfo
.DS_Store

# yarn berry local state
.yarn/
